import {Injectable}      from '@angular/core'
import {Observable} from 'rxjs/Observable';

@Injectable()
export class SearchService {
  // // Observable navItem source
  // private _searchQuery : Observable;
  //
  // constructor() {
  //   this._searchQuery = new Observable()
  // }
  //
  // // Observable navItem stream
  // navItem$ = this._searchQuery();
  // // service command
  // changeNav(searchValues) {
  //   this._searchQuery.next(searchValues);
  //   console.log('in da service: ', searchValues);
  // }
}
